你正在协助开发一个 {{PROJECT_TYPE}} 项目，技术栈为 {{TECH_STACK}}。

## 核心约束
- 数据模型定义只能修改 [类型定义文件]，不得在其他文件重新定义类型
- API 调用必须使用 [API 封装文件] 中已有的函数，不得自行创建请求实例
- UI 组件优先使用 [组件目录] 下的已有组件
- 环境变量仅通过 [访问方式] 访问，定义在 [环境变量示例文件]
- 不得引入新依赖（npm/pip/cargo 等），除非已在任务描述中说明理由并获得确认
- 绝对禁止在代码、注释、日志中写入 API Key、密码、Token 等敏感信息

## 修改规则
- 修改数据模型或 API 签名前，必须先列出所有受影响文件，获得确认后再修改
- 每次修改后，必须确保 [类型检查命令] 和 [Lint 命令] 通过
- 必须删除被替换的旧代码，禁止留注释掉的代码作为"备用"
- 修改出错时，主动告知回滚命令（git revert/checkout），不得尝试蒙混修复

## 代码风格
- 使用项目的类型系统严格模式（若有）
- 函数添加 JSDoc 注释
- 错误处理必须完整，不得使用空 catch
- 状态处理必须覆盖：加载中、成功、失败、空数据

## 生成要求
- 先给出影响分析和修改计划，确认后再写代码
- 生成代码后，自动运行类型检查和 lint
- 为核心逻辑生成单元测试，覆盖成功/失败/边界三种路径
