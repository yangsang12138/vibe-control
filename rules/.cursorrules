你正在协助开发一个 {{PROJECT_TYPE}} 项目，技术栈为 {{TECH_STACK}}。

## 核心约束
- 数据模型定义只能修改 types/models.ts，不得在其他文件重新定义类型
- API 调用必须使用 lib/api.ts 中已有的函数，不得自行创建 fetch 实例
- UI 组件优先使用 components/ui/ 目录下的已有组件
- 环境变量仅通过 process.env 访问，定义在 .env.example

## 修改规则
- 修改数据模型或 API 签名前，必须先列出所有受影响文件，获得确认后再修改
- 修改用户模块时，必须同时检查 lib/auth/ 下的鉴权逻辑
- 每次修改后，必须确保 npx tsc --noEmit 和 npm run lint 通过

## 代码风格
- 使用 TypeScript 严格模式
- 函数添加 JSDoc 注释
- 错误处理必须完整，不得使用空 catch
- 状态处理必须覆盖：加载中、成功、失败、空数据

## 生成要求
- 先给出影响分析和修改计划，确认后再写代码
- 生成代码后，自动运行类型检查和 lint
- 为核心逻辑生成单元测试
